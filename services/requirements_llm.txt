# -----------------------------------------------------------------------------
# LLM service requirements (Trainer T4 instance on Nuvolos -- GPU)
# -----------------------------------------------------------------------------
# Only what's needed to load and serve Qwen2.5-7B over HTTP.

transformers>=4.40.0
accelerate>=0.29.0
torch>=2.1.0
sentencepiece>=0.1.99
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
